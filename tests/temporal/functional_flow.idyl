import("stdlib")

functional() = [i = 0 .. 10 : i ]
func2() = [5, 4, 3, 2]

process: {
    flow_instance = func2()

    m = metro(dt=50ms)
    print( flow_instance[m]) // Should print the sine value at the current count index
}