lib("stdlib")
module("osc")

host = "127.0.0.1"
port = 9000

process: 
{
    handle = osc_out(host, port)
    mod = pos(lfo(1hz, dt=50ms)) * 1000 + 100 // modulating frequency between 100 and 1100 Hz 
    print(mod)
    osc_send(handle, "/csound/freq", mod)
}