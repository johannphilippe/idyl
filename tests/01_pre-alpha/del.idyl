lib("stdlib")

/*
del_test(dt=100ms) = count 
|> {
    init: {emit count = 0}
    emit count = count + 1
    emit b = '(count)
    emit d = '(count, 300ms)
}

process: {
    c = del_test()
    print(c, c::b, c::d)
}
*/

process: {
    a = counter(dt = 300ms) + 2
    b = '(a)
    c = '(a, 2)
    print(a, b, c)
}