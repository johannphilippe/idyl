lib("stdlib")

flow rhythm = [!, !, _, !, !, !, _, _, _, !, _, !, !, !]
printable(rhythm!) = rhythm
|> {
    print("trig", rhythm)
} 

trig(dt=100ms) = !

process: {
    m = metro(dt=200ms)
    r = rhythm[m]
    res = printable(r)
    print(res)


}