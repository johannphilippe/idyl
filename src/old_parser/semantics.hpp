#pragma once 

/*
    Semantics checking 
    - Type mismatch (cannot cast time to trigger, or type conversion rule )
    - Undeclared function 
    - Reserved identifier misuse 

    First Class Types : 
        - number (double by default)
        - time 
        - trigger 
        - module (csound.myinstr)
        - string (TODO)
*/
